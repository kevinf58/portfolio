// min and max length of a document's title
export const DOCUMENT_TITLE_MIN_LENGTH = 8;
export const DOCUMENT_TITLE_MAX_LENGTH = 60;

// min length of a document's content
export const DOCUMENT_CONTENT_MIN_LENGTH = 200;

// Limit to the number of documents that can be loaded at a time
export const DOCUMENTS_LOADED_LIMIT = 1;
